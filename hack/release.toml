# commit to be tagged for the new release
commit = "HEAD"

project_name = "talos-exporter"
github_repo = "shanduur/talos-exporter"
match_deps = "^github.com/(shanduur/[a-zA-Z0-9-]+)$"

# previous = -
# pre_release = true

# [notes]
